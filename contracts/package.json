{
  "name": "swiftsplit-contracts",
  "version": "1.0.0",
  "description": "SwiftSplit - Cross-border freelance payments on Arc",
  "main": "index.js",
  "scripts": {
    "compile": "hardhat compile",
    "deploy": "hardhat run scripts/deploy.js --network arcTestnet",
    "deploy:local": "hardhat run scripts/deploy.js --network localhost",
    "test": "hardhat test",
    "test:coverage": "hardhat coverage",
    "clean": "hardhat clean"
  },
  "keywords": ["arc", "usdc", "payments", "freelance", "blockchain"],
  "author": "SwiftSplit Team",
  "license": "MIT",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^3.0.0",
    "hardhat": "^2.17.0",
    "hardhat-gas-reporter": "^1.0.9"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^4.9.3"
  }
}